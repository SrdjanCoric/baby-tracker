appId: com.sofibaby.app

---
# Launch app fresh and skip onboarding to get to home screen
- launchApp:
    clearState: true

# Skip onboarding
- tapOn:
    text: "Skip"

# Wait for home screen
- extendedWaitUntil:
    visible:
      id: "home-screen"
    timeout: 5000
