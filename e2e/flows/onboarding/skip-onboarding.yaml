appId: com.sofibaby.app
name: Skip Onboarding Steps
tags:
  - regression
  - onboarding

---
- launchApp:
    clearState: true

# Welcome screen - skip
- assertVisible:
    id: "welcome-screen"
- tapOn:
    id: "skip-button"

# Should reach home directly (with default baby)
- assertVisible:
    id: "home-screen"
